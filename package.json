{
  "name": "pwa-status-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "jest",
    "build": "node scripts/prod.js",
    "dev": "node scripts/dev.js",
    "deploy": "node scripts/prod.js && npx firebase deploy --only hosting,database,functions",
    "deploy:dev": "node scripts/dev-deploy.js",
    "test:rules": "firebase emulators:exec --only database --project demo-on \"jest --config jest.rules.config.js --runInBand\"",
    "sync-shared": "node scripts/sync-shared.js",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.29.0",
    "@babel/preset-env": "^7.29.0",
    "@babel/preset-typescript": "^7.29.7",
    "@firebase/rules-unit-testing": "^5.0.1",
    "babel-jest": "^30.3.0",
    "canvas": "^3.2.1",
    "esbuild": "^0.27.4",
    "firebase-tools": "^15.11.0",
    "jest": "^30.3.0",
    "jest-environment-jsdom": "^30.3.0",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "firebase": "^12.10.0"
  }
}
